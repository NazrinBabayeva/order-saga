spring:
  kafka:
    template:
      order-create-event-topic: ${ORDER_CREATE_ORDER_TOPIC:order-events}
    consumer:
      group-id: saga-orchestrator-ms
      bootstrap-servers: localhost:9092
      auto-offset-reset: earliest

      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer

      properties:
        spring.json.trusted.packages: "com.example.orderorchestrator.event"
        spring.json.value.default.type: com.example.orderorchestrator.event.OrderCreatedEvent
        spring.json.use.type.headers: false

    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        acks: all
        retries: 10
        retry.backoff.ms: 1000
        max.block.ms: 20000
        linger.ms: 500
        delivery.timeout.ms: 35000
        max.request.size: 22020096
        spring:
          json:
            add:
              type:
                headers: false